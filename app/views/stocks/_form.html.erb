<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ticker %>
    <%= form.text_field :ticker, id: :stock_ticker %>
  </div>

  <div class="field">
    <%= form.label :purchaseprice %>
    <%= form.text_field :purchaseprice, id: :stock_purchaseprice %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :stock_quantity %>
  </div>

  <div class="field">
    <%= form.label :postion %>
    <%= form.text_field :postion, id: :stock_postion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
